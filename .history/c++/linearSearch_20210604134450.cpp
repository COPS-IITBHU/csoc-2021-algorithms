#include "bits/stdc++.h"
using namespace std;
int linearSearch(int arr[],int val,int n)
{
   for (int i = 0; i < n; i++)
   {
       if(arr[i]==val)
   }
   
}
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}