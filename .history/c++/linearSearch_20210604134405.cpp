#include "bits/stdc++.h"
using namespace std;
int linearSearch(int arr[],int val)
{
   for (int i = 0; i < ; i++)
   {
       /* code */
   }
   
}
int main()
{
   int n;
    cin >> n >> num;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}